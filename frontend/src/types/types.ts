export type LayoutProps = {
    children: React.ReactNode
}

export type NavBarSettings = {
    title: string,
    url: string
}

export type GetUSer = {
    id: number;
    username: string;
    email: string;
    createdDate: Date;
}

